apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: kubeflow-pipelines--restart-on-pipeline-configmap-update
spec:
  rules:
    - name: trigger-deployment-restarts
      match:
        any:
          - resources:
              kinds:
                - ConfigMap
              names:
                - "pipeline-install-config"
              namespaces:
                - "kubeflow"
              operations:
                - CREATE
                - UPDATE
      preconditions:
        all:
          - key: "{{ request.operation || 'BACKGROUND' }}"
            operator: AnyIn
            value: 
              - CREATE
              - UPDATE
      mutate:
        targets:
          - apiVersion: apps/v1
            kind: Deployment
            namespace: "{{ request.object.metadata.namespace }}"
        patchStrategicMerge:
          spec:
            template:
              metadata:
                annotations:
                  deploykf.org/restart-trigger: "{{ time_now_utc() }} | Kind: {{ request.object.kind }} | Name: {{ request.object.metadata.name }} | Namespace: {{ request.object.metadata.namespace }} | ResourceVersion: {{ request.object.metadata.resourceVersion }}"
              spec:
                containers:
                  - env:
                      - valueFrom:
                          configMapKeyRef:
                            <(name): "{{ request.object.metadata.name }}"